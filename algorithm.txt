Fsearch 1:
1.start a loop and run till the last index
2.check whether the current object key (season and number) matches with the parameter
3.if yes then convert the data to string and then print else give the appropriate message

Fsearch 2:
1.create an array and enter the ids 
2.start a loop and run till the last index
3.check whether the current object key (id) matches with the parameter
4.if yes then convert the data to string and then print else give the appropriate message

Fsearch 3:


1.start a loop and run till the last index
2.check whether the current object key (name) matches with the parameter
3.if yes then convert the data to string and then print else give the appropriate message
